//-
    Mixin select - Create a native <select> element for usage in forms. The outside is styled, but inside (dropdown itself) is native to OS.

mixin c-radio-group({ name, value = [A, B, C], valueKey, key, isInline = false, checked })
    .c-radio-group(class=isInline? "c-radio-group--inline" : "")
        each item in value
            .c-radio
                label
                    input(type="radio" value=item[valueKey] name=name checked=(item[valueKey]==checked) || (item==checked))
                    if key
                        | #{item[key]}
                    else
                        | #{item}
