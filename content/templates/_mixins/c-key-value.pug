//-
    Mixin Key value - Create a key value item

mixin c-key-value({ key, placeholder = 'Not entered' })
    .c-key-value-item
        dt.c-key-value-item__key= key
        dd.c-key-value-item__value
            if block
                block
            else
                span.u-text-muted #{placeholder}

