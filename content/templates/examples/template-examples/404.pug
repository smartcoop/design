extends /templates/_layouts/default

block append pageVariables
    - var moduleTitle = "404"

block content
    .c-error-page
        span.c-error-page__code 404
        h4.c-error-page__title Page not found
        p.c-error-page__desc Sorry, we couldn’t find the page you are looking for.
        +c-button({ icon: 'arrow-left', href: '/' }) Go back home
